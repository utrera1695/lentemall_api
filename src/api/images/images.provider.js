'use strict';

export default {
    async Save(file, folder) {
        try {
            const path = file.path;
            return path
        } catch (error) {
            throw error;
        }
    }
};